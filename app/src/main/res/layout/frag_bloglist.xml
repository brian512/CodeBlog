<com.brian.common.view.RefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/swipe_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="10dp"
    android:paddingRight="10dp">

    <ListView
        android:id="@+id/blogListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:dividerHeight="10dp"
        android:paddingTop="10dp"
        android:clipToPadding="false"
        android:fadingEdge="none"
        android:scrollbars="none" />

    <ImageView
        android:id="@+id/no_blog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:contentDescription="@string/str_null"
        android:src="@drawable/base_empty_view"
        android:visibility="gone" />
</com.brian.common.view.RefreshLayout>